import init from 'bobbinbear_core';

type LazyCanvasProps = {

}
async function LazyRenderer = (props: LazyCanvasProps) => {
    const 
    return <div ref={containerEl} class="w-full h-full">
    </div>
}

export function Renderer(props: RendererProps) {
}
